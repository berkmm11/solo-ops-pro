import AppLayout from "@/components/AppLayout";

const Projects = () => (
  <AppLayout>
    <div>
      <h1 className="text-2xl font-semibold text-foreground">Projeler</h1>
      <p className="mt-2 text-muted-foreground">Henüz proje eklenmedi.</p>
    </div>
  </AppLayout>
);

export default Projects;
