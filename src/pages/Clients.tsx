import AppLayout from "@/components/AppLayout";

const Clients = () => (
  <AppLayout>
    <div>
      <h1 className="text-2xl font-semibold text-foreground">Müşteriler</h1>
      <p className="mt-2 text-muted-foreground">Henüz müşteri eklenmedi.</p>
    </div>
  </AppLayout>
);

export default Clients;
